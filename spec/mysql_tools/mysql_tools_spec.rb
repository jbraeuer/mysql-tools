require 'spec_helper'

module MysqlTools
  describe Backup do
    describe "#run" do
      before(:each) do
      end

      it "backups the given table"

      it "it fails, when the backup could not be created."

    end
  end
end
